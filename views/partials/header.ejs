<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sorting Randomize Array</title>
    <style>
        /* Define CSS styles for the bars */
        .bar {
        width: 20px;            /* Width of each bar */
        margin: 5px;            /* Margin between bars */
        display: inline-block;  /* Display bars in a row */
        background-color: lightblue; /* Bar color */
        color: gray;           /* Text color */
        text-align: center;     /* Center text inside the bar */
    }
      </style>
</head>
<body>